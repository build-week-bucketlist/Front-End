import {} from './SignInAction'

const initialState = {
    todo:[],
    fetchingList:false,
    error:null
}

export const signinReducer = (state = initialState, action) => {
    switch ( action.type){
        default:
            return state;
    }
}
